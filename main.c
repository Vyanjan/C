#include <stdio.h>

void main() {
    system("cls");
    // printf("Hello World");

    char name[10], surname[10];
    scanf("%s %s", name, surname);

    printf("Hello %s %s", name, surname);

    return;
}